<?php
  function pole($a) {
    return number_format((6 * $a),2);
  }
  function objetosc($a) {
    return number_format(($a * $a * $a),2);
  }
  function przekatna($a) {
    return number_format(($a * sqrt(3)),2);
  }
  function promien_kuli_wpisanej($a) {
    return number_format(((1 / 2) * $a),2);
  }
  function promien_kuli_opisanej($a) {
    return number_format(((1 / 2) * $a * sqrt(3)),2);
  }
?> 